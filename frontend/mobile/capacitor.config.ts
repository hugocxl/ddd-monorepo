import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sygris.app',
  appName: '@sygris/mobile',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
